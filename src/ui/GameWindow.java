package ui;

import java.awt.*;
import javax.swing.*;

import engine.Game;

public class GameWindow extends JFrame {
    public GameWindow(Config cfg) {
        int games = cfg.getGames();

        //Game game = new Game();
    }
}
